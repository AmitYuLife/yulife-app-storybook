import { Animated, Easing as RNEasing } from "react-native";

export const initializeRegistryWithDefinitions = () => {};
export const Easing = RNEasing;
export const useAnimatedStyle = (f) => f()
export const withTiming = value => value.value;

export default Animated;
