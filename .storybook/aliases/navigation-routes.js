/** Minimal routes stub for Storybook — avoids importing every screen container at module init. */
const routes = [];

export default routes;

export const dynamicallyRegisteredRoutes = [];

export const preRegisteredRoutes = [];
