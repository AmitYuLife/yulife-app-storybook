import { navigation } from "@utils";
export { authoriseFitkit, sendSteps } from "@socket";

export const { tapText, reloadAppToTab, tapID, typeViaID, wait, reloadOnly } = navigation.common;

export const { dismissNewLooksModalIfVisible, skipHealthConnection } = navigation.login;
