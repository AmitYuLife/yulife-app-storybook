import { navigation } from "@navigation"

export const {
    logInAndGoToTab,
    loginAsUser,
    loginOnly,
    loginAndCollectSignupBonus,
    loginToYuScreen,
    loginAsPLIUser,
} = navigation.login

export const {
    navigateViaID,
    navigateViaText,
} = navigation.common
