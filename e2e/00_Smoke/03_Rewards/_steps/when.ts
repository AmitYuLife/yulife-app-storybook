import { screens } from "@appScreens"
import { navigation } from "@utils"


export const {
    tapRewardInList,
    tapDenomination,
    tapDenominationList,
    tapBuyButton,
    tapPurchasedReward
} = screens.rewards

export const {
    scrollFromText,
} = navigation.scrolling

export const {
    tapText,
    reloadAppToTab
} = navigation.common