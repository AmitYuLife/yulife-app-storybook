import { navigation } from "@navigation"
import { screens } from "@appScreens"

export const {
    completeIntro,
} = navigation.login

export const {
    tapID,
    tapText,
    tapIDAtPoint
} = navigation.common

export const {
    tapMenuItem
} = screens.menu

export const {
    tapRewardInList
} = screens.rewards