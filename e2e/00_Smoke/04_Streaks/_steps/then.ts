import { navigation } from "@utils"
import { screens } from "@appScreens"

export const {
    idVisible,
    idNotVisible,
    textVisible,
    textNotVisible,
    multipleTextVisible,
} = navigation.common

export const {
    onStartStreak,
} = screens.streaks

export const {
    onChallengeComplete,
} = screens.challenges