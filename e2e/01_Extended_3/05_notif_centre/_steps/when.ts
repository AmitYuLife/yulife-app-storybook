
import { navigation } from "@utils";
export { authoriseFitkit, sendSteps } from "@socket";

export const {
  tapText,
  tapID,
  wait,
  navigateTo,
  tapYuCoinIcon,
  navigateViaID
} = navigation.common;