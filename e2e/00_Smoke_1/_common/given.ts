import { navigation } from "@navigation";

export const {
    loginOnly,
    loginAsUser,
    logInAndGoToTab,
    loginAndCollectSignupBonus,
    selectRegionIfVisible,
} = navigation.login

export const {
    wait,
} = navigation.common
