/**
 * Just a placeholder, not needed when not in e2e
 */
import SocketClient from "@mockclient";
console.log("Socket client");
export default new SocketClient();

export const DETOX_ENABLED = false;
