import { Colours } from "@styles";
import { TextStyle } from "react-native";

export const styles = {
  bold: {
    fontSize: 20,
    lineHeight: 24,
    letterSpacing: 1,
    color: Colours.products.fib.n800,
  } as TextStyle,
  margin: {
    marginTop: 24,
  },
};
