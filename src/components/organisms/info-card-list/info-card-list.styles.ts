import { StyleSheet } from "react-native";
import { Style } from "@styles";

export default StyleSheet.create({
  taskWrapper: { marginBottom: Style.adjust(16) },
  taskTitle: { marginTop: Style.adjust(8), marginBottom: 0 },
});
