import { StyleSheet, ViewStyle } from "react-native";

export default StyleSheet.create({
    flex: {
        flex: 1
    } as ViewStyle,
    wrapper: {
        flex: 1,
        marginBottom: -2
    } as ViewStyle
});
