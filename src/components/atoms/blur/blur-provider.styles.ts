import { ViewStyle } from "react-native";

import { StyleSheet } from "@styles";
export default StyleSheet.create({
  flex: {
    flex: 1,
  } as ViewStyle,
  wrapper: {
    flex: 1,
  } as ViewStyle,
});
