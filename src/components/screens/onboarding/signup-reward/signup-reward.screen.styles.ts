import {
    StyleSheet,
    TextStyle
} from "react-native";
import { Style } from "../../../../styles";

export default StyleSheet.create({
    heading: {
        marginTop: Style.SCALE_UP_AND_DOWN(-10)
    } as TextStyle
});
