import { ImageStyle, StyleSheet, ViewStyle } from "react-native";

export default StyleSheet.create({
    backgroundImage: {
        bottom: 0,
        left: 0,
        position: "absolute",
        right: 0,
        width: "100%"
    } as ImageStyle,
    wrapper: {
        flex: 1
    } as ViewStyle
});
