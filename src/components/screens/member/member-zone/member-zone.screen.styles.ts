import { StyleSheet, ViewStyle } from "react-native";

export default StyleSheet.create({
    invisible: {
        opacity: 0
    } as ViewStyle,
    webView: {
        borderColor: "red",
        borderWidth: 1,
        flex: 1,
        marginTop: 60
    } as ViewStyle
});
