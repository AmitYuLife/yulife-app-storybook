import { Colours, Style } from "@styles";

export const SHARED = {
  PADDING_LEFT: Style.adjust(24),
  BORDER: {
    borderWidth: 1,
    borderColor: Colours.neutral.n100,
    borderRadius: 8,
  },
};
