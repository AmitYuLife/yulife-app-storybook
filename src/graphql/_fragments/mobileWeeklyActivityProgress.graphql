fragment MobileWeeklyActivityProgress on MobileWeeklyActivityProgress {
  id
  activitySubTotal
  yuCoinSubTotal
  currentPosition
  maxLength
  isClaimable
  isClaimed
  isJoined
  iconUrl {
    id
    uri
  }
}
