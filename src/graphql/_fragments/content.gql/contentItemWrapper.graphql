fragment ContentItemWrapper on ContentItemWrapper {
  id
  styles {
    ...SduiStyle
  }
  children
  pointerEvents
  absolute
  onPress {
    ...SduiAction
  }
  scrollViewProps
  gestureViewProps
  sharedValue
  dynamicStyleKey
  localDispatchActions {
    ...SduiAction
  }
  localDispatchActionsOnMount {
    ...SduiAction
  }
}
