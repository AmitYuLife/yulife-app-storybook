query GetAdBanners($place: String) {
  getAdBanners(place: $place) {
    __typename
    id
    imageUrl {
      uri
    }
    navigateTo
    navigateToType
    routeProps
    startDate
    endDate
  }
}
