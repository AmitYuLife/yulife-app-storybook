mutation UpdateAvatar($avatar: [UserAvatarPartUpdate]) {
  updateUserAvatarParts(avatar: $avatar) {
    rewarded
    rewardAmount
    updated
    avatarRemoteFiles {
      ...YumojiRemoteFiles
    }
  }
}
