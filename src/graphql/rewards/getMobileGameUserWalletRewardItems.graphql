query GetMobileGameUserWalletRewardItems($type: String, $rewardId: String!, $offset: Int, $markedAsUsed: Boolean) {
  getMobileGameUserWalletRewardItems(type: $type, rewardId: $rewardId, offset: $offset, markedAsUsed: $markedAsUsed) {
    sections {
      ...UserWalletSection
    }
    image {
      ...RemoteImage
    }
  }
}
