fragment ReferralSectionContent on ReferralSectionContent {
  illustration {
    ...VariableRemoteImage
  }
  title
  markdown
  buttonLabel
  buttonIcon {
    ...RemoteImage
  }
}

fragment ReferralSection on ReferralSection {
  __typename
  id
  sectionInstanceId
  ready
  updateOnView
  loadingGroup
  content {
    ...ReferralSectionContent
  }
}
