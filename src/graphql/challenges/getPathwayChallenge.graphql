query GetPathwayChallenge {
  getPathwayChallenge {
    id
    challengeId
    heading
    isLocked
    isCompleted
    isStarted
    reward
    expiresAt
    action {
      ...SduiAction
    }
  }
}
