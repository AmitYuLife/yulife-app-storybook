include:
  - local: "gitlab/*.yml"
  - project: "yu-life/infrastructure/yulife-devops-shared-config"
    ref: main
    file: "shared-gitlab-config.yml"

variables:
  ROLE_ARN: arn:aws:iam::846880112034:role/oidc/OIDC-gitlab-develop-react-native-client

stages:
  - build
  - test
  - release
  - translate
