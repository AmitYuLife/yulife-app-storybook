import React from "react";
import { View } from "react-native";

export const AirplayButton = (props) => React.createElement(View, props);
export const showRoutePicker = () => {};
export const useExternalPlaybackAvailability = () => false;
export const useAirplayConnectivity = () => false;
export default { AirplayButton, showRoutePicker, useExternalPlaybackAvailability, useAirplayConnectivity };
